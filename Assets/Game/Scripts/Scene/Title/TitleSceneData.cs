using GameFramework.Scene;

namespace Game.Scripts.Scene.Title
{
    public class TitleSceneData : SceneData
    {
        public TitleSceneData()
        {
            this.name = "Title";
            this.type = typeof(TitleScene);
        }
    }
}
